[project]
name = "npc-persona"
version = "0.1.0"
description = "System-agnostic and setting-agnostic NPC generator"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0.3",
]
